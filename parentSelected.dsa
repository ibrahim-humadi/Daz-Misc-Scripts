// DAZ Studio version 4.22.0.1 filetype DAZ Script

//select the parent first and the childToBe second
(function(){
var nodes = Scene.getSelectedNodeList();

var childToBe = nodes[1];
var oldParent = ndoes[1].getNodeParent();
var parentToBe = nodes[0];

oldParent.removeNodeChild(childToBe,true);
parentToBe.addNodeChild( childToBe, true); 

})();